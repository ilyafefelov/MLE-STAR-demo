# Абляційний аналіз та статистична оцінка важливості компонентів багатостадійного ML-конвеєра MLE-STAR

> 🧠 Дипломна робота магістра МАУП  
> Студент: **Фефелов Ілля Олександрович**  
> Група: **ІК-9-24-М1ІПЗ (1.6д)-ІТ**  
> Науковий керівник: **к.т.н. Дмитро Чернишов**

## Опис проєкту

Цей проєкт досліджує вплив окремих компонентів ML-конвеєра на якість моделей машинного навчання через абляційний аналіз. **Основна ідея:**

1. **Використовуємо MLE-STAR** (Multi-agent LLM Environment STAR) для генерації оптимального ML-пайплайна
2. **Проводимо абляційний аналіз**: по черзі вимикаємо компоненти пайплайна
3. **Статистично оцінюємо** внесок кожного компонента (t-test, ANOVA, Cohen's d)
4. **Формуємо рекомендації** щодо необхідності кожного етапу конвеєра

### Що таке MLE-STAR?

**MLE-STAR** - це мультиагентна система на базі LLM, яка автоматично:
- Аналізує датасет та визначає тип задачі
- Генерує оптимальний ML-пайплайн (код на Python)
- Використовує пошук + targeted refinement для покращення рішення
- Підбирає гіперпараметри та створює ансамблі

### Наш внесок

Ми **не замінюємо MLE-STAR**, а доповнюємо його інструментарієм для:
- Систематичного вимикання компонентів пайплайна
- Багаторазового запуску кожної конфігурації
- Статистичної оцінки значущості різниць
- Візуалізації результатів

**Важливо:** Це дослідницький проєкт, який **аналізує** результати MLE-STAR, а не модифікує сам агент.

## Структура проєкту

```
├── README.md                   # Цей файл
├── COPILOT_INSTRUCTIONS.md     # Інструкції для GitHub Copilot
├── requirements.txt            # Залежності Python
├── src/
│   └── mle_star_ablation/      # Основні модулі
│       ├── __init__.py
│       ├── pipelines.py        # Конфігурації ML-конвеєрів
│       ├── datasets.py         # Завантаження та підготовка даних
│       ├── metrics.py          # Обчислення метрик
│       ├── stats.py            # Статистичний аналіз
│       └── viz.py              # Візуалізація результатів
├── scripts/
│   ├── run_ablation.py         # CLI для запуску експериментів
│   └── run_single_experiment.py # Запуск одиничної конфігурації
├── configs/
│   └── ablation_config.yaml    # Конфігурація експериментів
├── results/                     # Результати експериментів
└── notebooks/                   # Jupyter ноутбуки для аналізу
```

## Швидкий старт

### Варіант A: З реальним MLE-STAR (для диплома)

#### 1. Встановлення

```bash
# Основні залежності
pip install -r requirements.txt

# MLE-STAR агент
pip install mle-star

# API ключ для LLM
export ANTHROPIC_API_KEY="your_api_key_here"
```

#### 2. Генерація пайплайна через MLE-STAR

```bash
python scripts/run_mle_star.py \
  --dataset data/your_data.csv \
  --task-type classification \
  --search-iterations 1
```

**Це займе 2-4 години.** MLE-STAR згенерує оптимальний пайплайн.

#### 3. Абляційний аналіз

```bash
python scripts/run_ablation.py \
  --mle-star-pipeline mle_star_output/mle_star_pipeline.py \
  --dataset data/your_data.csv \
  --n-runs 5
```

Результати: `results/` (CSV, графіки, статистичний звіт)

---

### Варіант B: Швидкий тест (без MLE-STAR)

Для тестування фреймворку на вбудованих датасетах:

```bash
pip install -r requirements.txt

python scripts/run_ablation.py --dataset breast_cancer --n-runs 3
```

**Увага:** Це використовує стандартні sklearn пайплайни, НЕ MLE-STAR. Для диплома потрібен Варіант A.

## Основні компоненти

- **Масштабування даних**: StandardScaler, MinMaxScaler
- **Інженерія ознак**: PolynomialFeatures, взаємодії
- **Вибір моделі**: LogisticRegression, RandomForest, GradientBoosting
- **Тюнінг гіперпараметрів**: GridSearchCV
- **Енсамблювання**: VotingClassifier, StackingClassifier

## Результати

Результати експериментів зберігаються в папці `results/` у форматі CSV та PNG (графіки).

## База практики

**ТОВ "ГОУ АЙТІ ЕДЬЮКЕЙШЕН"**

## Документація

- [MLE_STAR_INTEGRATION.md](MLE_STAR_INTEGRATION.md) - **Детальна інтеграція з MLE-STAR**
- [QUICKSTART.md](QUICKSTART.md) - Швидкий старт
- [COPILOT_INSTRUCTIONS.md](COPILOT_INSTRUCTIONS.md) - Інструкції для GitHub Copilot
- `notebooks/example_ablation_analysis.ipynb` - Приклад використання

## Ключові файли

### MLE-STAR інтеграція
- `src/mle_star_ablation/mle_star_wrapper.py` - Обгортка для запуску агента
- `src/mle_star_ablation/mle_star_adapter.py` - Адаптер для згенерованих пайплайнів
- `scripts/run_mle_star.py` - CLI для генерації пайплайна

### Абляційний аналіз
- `src/mle_star_ablation/pipelines.py` - Управління конфігураціями
- `src/mle_star_ablation/datasets.py` - Завантаження даних
- `scripts/run_ablation.py` - Запуск експериментів

### Статистика та візуалізація
- `src/mle_star_ablation/stats.py` - t-test, ANOVA, Cohen's d
- `src/mle_star_ablation/viz.py` - Графіки
- `src/mle_star_ablation/metrics.py` - Метрики

## Для диплому

**Теза дослідження:**  
MLE-STAR генерує складні ML-пайплайни, але незрозуміло, наскільки кожен компонент важливий. Наш абляційний аналіз дозволяє кількісно оцінити внесок масштабування, інженерії ознак, тюнінгу та енсамблювання.

**Результат:**  
Статистично обґрунтовані рекомендації, які компоненти критичні для якості моделі.

**Унікальність:**  
Перше дослідження абляційного аналізу саме для MLE-STAR (AutoML на базі LLM).

## Контакти

Фефелов Ілля Олександрович  
Група: ІК-9-24-М1ІПЗ (1.6д)-ІТ  
МАУП, 2025
