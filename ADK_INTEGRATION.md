# Інтеграція з офіційним Google ADK MLE-STAR

## 🎯 Концепція

Цей проєкт використовує **офіційний MLE-STAR агент від Google** як джерело ML-конвеєрів, а **наш фреймворк** - для їхнього абляційного аналізу.

```
┌──────────────────────────────────────────────────────────┐
│  Google ADK MLE-STAR                                      │
│  (github.com/google/adk-samples)                          │
│  ────────────────────────────────────────────────────────│
│  • Multi-agent LLM system                                 │
│  • Generates ML pipeline code                             │
│  • Uses Gemini API                                        │
│                                                            │
│  Input:  dataset + task description                       │
│  Output: mle_star_pipeline.py                             │
└──────────────────────────────────────────────────────────┘
                         ↓
┌──────────────────────────────────────────────────────────┐
│  НАШ ФРЕЙМВОРК (Diploma Project)                         │
│  ────────────────────────────────────────────────────────│
│  • Ablation analysis                                      │
│  • Statistical evaluation                                 │
│  • Component importance assessment                        │
│                                                            │
│  Input:  mle_star_pipeline.py + dataset                   │
│  Output: statistical_report.txt + visualizations          │
└──────────────────────────────────────────────────────────┘
```

---

## 📦 Що таке Google ADK MLE-STAR?

**MLE-STAR** (Machine Learning Engineering STAR) - це мультиагентна LLM-система від Google для автоматичної розробки ML-конвеєрів.

### Репозиторій
- **GitHub:** https://github.com/google/adk-samples
- **Шлях:** `python/agents/machine-learning-engineering/`
- **Документація:** https://ai.google.dev/gemini-api/docs/agent-developer-kit

### Архітектура
MLE-STAR складається з кількох спеціалізованих агентів:
- **Planner** - планує підхід до задачі
- **Retriever** - шукає релевантну інформацію
- **Evaluator** - оцінює якість рішення
- **Ensemble** - комбінує множинні підходи
- **Debug** - виправляє помилки

### Workflow
1. Користувач надає датасет та опис задачі
2. Агенти колаборують через Gemini API
3. Генерується Python код з ML-конвеєром
4. Код автоматично тестується на даних

---

## 🚀 Інтеграція: покроковий план

### Крок 1: Налаштування ADK MLE-STAR

#### 1.1. Клонувати репозиторій
```bash
cd d:\School\GoIT\MAUP_REDO_HWs\
git clone https://github.com/google/adk-samples.git
cd adk-samples/python/agents/machine-learning-engineering
```

#### 1.2. Встановити залежності
```bash
# Створити окреме середовище для ADK (опціонально)
python -m venv venv_adk
venv_adk\Scripts\activate  # Windows

# Встановити ADK
pip install google-genai
pip install -r requirements.txt  # якщо є
```

#### 1.3. Налаштувати API ключ
```bash
# Отримати API ключ: https://aistudio.google.com/app/apikey
$env:GEMINI_API_KEY = "your_api_key_here"
```

#### 1.4. Запустити демо
```bash
# Приклад запуску (команда може відрізнятися, див. README ADK)
python run_mle_agent.py --task classification --dataset example_data.csv
```

**Очікуваний результат:** файл `mle_star_pipeline.py` з згенерованим ML-кодом.

---

### Крок 2: Витягти згенерований pipeline

Після успішного запуску MLE-STAR:

#### 2.1. Знайти згенерований код
```bash
# ADK зберігає результати у вихідній папці (зазвичай 'output/' або 'results/')
ls output/
# Шукаємо файл на кшталт: mle_star_pipeline.py, generated_model.py, тощо
```

#### 2.2. Проінспектувати структуру
```python
# Приклад згенерованого коду (спрощено):
from sklearn.pipeline import Pipeline
from sklearn.preprocessing import StandardScaler
from sklearn.ensemble import RandomForestClassifier

def create_pipeline():
    return Pipeline([
        ('scaler', StandardScaler()),
        ('model', RandomForestClassifier()),
    ])
```

#### 2.3. Скопіювати у наш проєкт
```bash
# Копіюємо згенерований код
cp adk-samples/.../output/mle_star_pipeline.py \
   d:/School/GoIT/MAUP_REDO_HWs/Diploma/data/mle_star_pipelines/
```

---

### Крок 3: Адаптувати для абляції

#### 3.1. Відкрити `mle_star_generated_pipeline.py`
```python
# src/mle_star_ablation/mle_star_generated_pipeline.py

def build_full_pipeline() -> Pipeline:
    """
    ⚠️ ЗАМІНИТИ ЦЕЙ БЛОК НА КОД ВІД MLE-STAR!
    """
    # Вставити сюди код з mle_star_pipeline.py
    # Приклад:
    
    from sklearn.preprocessing import StandardScaler
    from sklearn.decomposition import PCA
    from sklearn.ensemble import RandomForestClassifier
    from sklearn.pipeline import Pipeline
    
    # Це реальний код, згенерований MLE-STAR:
    pipeline = Pipeline([
        ('scaler', StandardScaler()),           # Масштабування
        ('feature_eng', PCA(n_components=10)),  # Інженерія ознак
        ('model', RandomForestClassifier()),    # Модель
    ])
    
    return pipeline
```

#### 3.2. Оновити `_STEP_NAMES`
```python
# У тому ж файлі: маппінг назв кроків
_STEP_NAMES = {
    'scaler': 'scaler',           # Назва з MLE-STAR
    'feature_engineering': 'feature_eng',  # Назва з MLE-STAR
    'model': 'model',
}
```

#### 3.3. Перевірити сумісність
```bash
cd d:\School\GoIT\MAUP_REDO_HWs\Diploma
python -c "from src.mle_star_ablation.mle_star_generated_pipeline import build_full_pipeline; print(build_full_pipeline())"
```

---

### Крок 4: Запустити абляційний аналіз

#### 4.1. Підготувати скрипт
```python
# scripts/run_ablation_on_mle_star.py
from sklearn.datasets import load_breast_cancer
from src.mle_star_ablation.config import get_standard_configs
from src.mle_star_ablation.ablation_runner import run_ablation_suite

# Завантажити дані
X, y = load_breast_cancer(return_X_y=True)

# Запустити абляцію
configs = get_standard_configs()
results = run_ablation_suite(
    X, y, 
    configs=configs,
    n_folds=5,
    n_repeats=3,
    verbose=True
)

# Зберегти результати
results.to_csv('results/mle_star_ablation.csv', index=False)
print("\n✅ Результати збережено у results/mle_star_ablation.csv")
```

#### 4.2. Запустити
```bash
python scripts/run_ablation_on_mle_star.py
```

#### 4.3. Проаналізувати результати
```python
# scripts/analyze_ablation_results.py
import pandas as pd
from src.mle_star_ablation.ablation_runner import summarize_results, compare_to_baseline

df = pd.read_csv('results/mle_star_ablation.csv')

# Агрегована статистика
summary = summarize_results(df)
print(summary)

# Порівняння з baseline
comparison = compare_to_baseline(df, baseline_name='full')
print("\n📊 Вплив компонентів:")
print(comparison[['config_name', 'delta_pct']])
```

---

## 📊 Очікувані результати

### Приклад output від абляції

```
====================================================================
ЗАПУСК АБЛЯЦІЙНОГО АНАЛІЗУ
====================================================================
Конфігурацій: 6
Повторів: 3
Фолдів: 5
Всього експериментів: 18
====================================================================

[1/6] Конфігурація: full
  Повний pipeline з усіма компонентами (baseline)
  Повтор 1/3 (seed=42)... ✓ accuracy: 0.9649
  Повтор 2/3 (seed=43)... ✓ accuracy: 0.9614
  Повтор 3/3 (seed=44)... ✓ accuracy: 0.9649

[2/6] Конфігурація: no_scaling
  Pipeline без масштабування ознак
  Повтор 1/3 (seed=42)... ✓ accuracy: 0.9298
  ...

====================================================================
ЗАВЕРШЕНО: 18 експериментів
Успішних: 18/18
====================================================================

📊 Вплив компонентів:
config_name         delta_pct
full                0.00%
no_tuning          -0.52%
no_ensemble        -1.23%
no_feature_eng     -2.15%
no_scaling         -3.87%
minimal            -8.42%
```

---

## 🎓 Для дипломної роботи

### Теоретична частина

**Розділ 2.1: Google ADK MLE-STAR**
- Архітектура мультиагентної системи
- Роль LLM (Gemini) у генерації коду
- Порівняння з іншими AutoML (Auto-sklearn, TPOT)
- Переваги та обмеження

**Розділ 2.2: Методологія абляційного аналізу**
- Систематичне вимикання компонентів
- Статистична оцінка впливу (t-test, ANOVA, Cohen's d)
- K-fold cross-validation для робастності

### Практична частина

**Розділ 3: Реалізація**
```python
# Приклад коду для тексту диплому:

# 1. Генерація базового пайплайна (офіційний MLE-STAR)
# (запуск через Google ADK)

# 2. Імпорт згенерованого пайплайна
from mle_star_generated_pipeline import build_full_pipeline
baseline_pipeline = build_full_pipeline()

# 3. Створення абляційних конфігурацій
configs = [
    AblationConfig(name="full"),  # baseline
    AblationConfig(name="no_scaling", use_scaling=False),
    AblationConfig(name="no_features", use_feature_engineering=False),
    ...
]

# 4. Запуск експериментів
results = run_ablation_suite(X, y, configs, n_repeats=5)

# 5. Статистичний аналіз
report = generate_statistical_report(results)
```

**Розділ 4: Експерименти**
- Датасети: breast_cancer, wine, digits
- Конфігурації: 6 варіантів × 5 повторів × 5 фолдів
- Метрики: accuracy, F1, ROC-AUC, час тренування
- Статистичні тести: paired t-test, Bonferroni correction

### Висновки

✅ **Що можна стверджувати:**
- "Використано офіційний Google ADK MLE-STAR для генерації базового пайплайна"
- "Розроблено фреймворк для абляційного аналізу його компонентів"
- "Проведено статистичну оцінку значущості кожного компонента"
- "Сформовано рекомендації щодо необхідності компонентів"

❌ **Чого НЕ можна стверджувати:**
- "Вдосконалили MLE-STAR" (не вдосконалювали)
- "MLE-STAR автоматично проводить абляцію" (не проводить)

---

## 🛠️ Альтернативи (якщо ADK не запрацює)

### План Б: Mock режим
Якщо ADK складно налаштувати, наш фреймворк має **mock режим**:

```python
# src/mle_star_ablation/mle_star_generated_pipeline.py
# вже містить шаблонний pipeline для тестування
```

### План В: Ручна адаптація прикладу
```bash
# Взяти приклад з ADK документації
curl https://example.com/mle_star_example.py > data/mle_star_example.py
# Адаптувати вручну
```

### План Г: Використати pipelines з Auto-sklearn
```python
import autosklearn.classification as auto
automl = auto.AutoSklearnClassifier()
automl.fit(X_train, y_train)
# Витягти найкращий pipeline
pipeline = automl.show_models()
```

---

## ✅ Checklist для інтеграції

- [ ] Клоновано `google/adk-samples`
- [ ] Встановлено Gemini API ключ
- [ ] Запущено MLE-STAR на демо-датасеті
- [ ] Отримано згенерований `mle_star_pipeline.py`
- [ ] Скопійовано код у `build_full_pipeline()`
- [ ] Оновлено `_STEP_NAMES` відповідно до реальних назв
- [ ] Перевірено, що pipeline імпортується без помилок
- [ ] Запущено `run_ablation_suite()` на тестових даних
- [ ] Проаналізовано результати та згенеровано звіти
- [ ] Додано скріншоти/лори для диплому

---

## 📚 Корисні посилання

- **Google ADK GitHub:** https://github.com/google/adk-samples
- **Gemini API Docs:** https://ai.google.dev/gemini-api/docs
- **MLE-STAR Paper:** [посилання на статтю, якщо є]
- **Наш репозиторій:** d:\School\GoIT\MAUP_REDO_HWs\Diploma

---

**Автор:** Фефелов Ілля Олександрович  
**МАУП, 2025**
